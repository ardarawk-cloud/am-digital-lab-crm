{
  "name": "am-digital-lab-crm",
  "version": "0.2.1",
  "private": true,
  "description": "AM DIGITAL LAB CRM & Internal Dashboard",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && for f in lib/*.js public/*.js; do node --check \"$f\"; done"
  },
  "engines": {"node": ">=22"}
}
